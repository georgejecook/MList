import "pkg:/source/roku-list/ItemScroller.bs"

class ListItemScroller extends ItemScroller
  function new(group, minPos, maxPos, moveAnimation, moveAnimationInterpolator)
    super(group, 1, minPos, maxPos, moveAnimation, moveAnimationInterpolator)
  end function

end class
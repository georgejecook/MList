import "pkg:/source/roku-list/ItemScroller.bs"

class ListItemScroller extends ItemScroller
  function new(group, axes, minPos, maxPos, moveAnimation, moveAnimationInterpolator)
    super(group, axes, minPos, maxPos, moveAnimation, moveAnimationInterpolator)
  end function

end class
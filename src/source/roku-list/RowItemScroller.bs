import "pkg:/source/roku-list/ItemScroller.bs"

class RowItemScroller extends ItemScroller
  function new(group, minPos, maxPos, moveAnimation, moveAnimationInterpolator)
    super(group, 0, minPos, maxPos, moveAnimation, moveAnimationInterpolator)
  end function
end class